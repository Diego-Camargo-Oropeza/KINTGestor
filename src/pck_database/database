-- COMPAT con XAMPP/MariaDB
SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS; SET UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS; SET FOREIGN_KEY_CHECKS=0;
-- Evita forzar un SQL_MODE no soportado por MariaDB
-- (Usa el que tenga tu servidor o uno básico)
-- SET @OLD_SQL_MODE=@@SQL_MODE; SET SQL_MODE='NO_ENGINE_SUBSTITUTION';

-- -----------------------------------------------------
-- Schema kint_inv
-- -----------------------------------------------------
DROP SCHEMA IF EXISTS `kint_inv`;
CREATE SCHEMA IF NOT EXISTS `kint_inv`
  DEFAULT CHARACTER SET utf8mb4
  COLLATE utf8mb4_general_ci;
USE `kint_inv`;

-- -----------------------------------------------------
-- Table producto
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `producto` (
  `id_producto` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `sku` VARCHAR(80) NOT NULL,
  `nombre` VARCHAR(160) NOT NULL,
  `descripcion` TEXT NULL DEFAULT NULL,
  `tipo` ENUM('MATERIAL', 'HERRAMIENTA', 'DISPOSITIVO') NOT NULL,
  `marca` VARCHAR(120) NULL DEFAULT NULL,
  `categoria` VARCHAR(120) NULL DEFAULT NULL,
  `u_medida` VARCHAR(20) NULL DEFAULT NULL,
  `codigo_barras` VARCHAR(128) NOT NULL,
  `activo` TINYINT(1) NOT NULL DEFAULT '1',
  `fecha_creacion` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `fecha_edicion` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id_producto`),
  UNIQUE INDEX `sku` (`sku` ASC)
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_general_ci;

-- -----------------------------------------------------
-- Table rol
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `rol` (
  `id_rol` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `nombre` VARCHAR(50) NOT NULL,
  `descripcion` VARCHAR(255) NULL DEFAULT NULL,
  PRIMARY KEY (`id_rol`),
  UNIQUE INDEX `nombre` (`nombre` ASC)
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_general_ci;

-- -----------------------------------------------------
-- Table usuario
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `usuario` (
  `id_usuario` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `id_rol` INT UNSIGNED NOT NULL,
  `nombre` VARCHAR(120) NOT NULL,
  `correo` VARCHAR(160) NOT NULL,
  `contrasena_hash` VARCHAR(255) NOT NULL,
  `activo` TINYINT NOT NULL DEFAULT '1',
  `fecha_creacion` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `fecha_actualizacion` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id_usuario`),
  UNIQUE INDEX `correo` (`correo` ASC),
  INDEX `fk_usuario_rol` (`id_rol` ASC),
  CONSTRAINT `fk_usuario_rol`
    FOREIGN KEY (`id_rol`) REFERENCES `rol` (`id_rol`)
    ON UPDATE CASCADE ON DELETE RESTRICT
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_general_ci;

-- -----------------------------------------------------
-- Table solicitud
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `solicitud` (
  `id_solicitud` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `id_usuario` INT UNSIGNED NOT NULL,
  `folio` VARCHAR(40) NOT NULL,
  `estado` ENUM('BORRADOR', 'ENVIADA', 'APROBADA', 'RECHAZADA', 'ENTREGADA') NOT NULL DEFAULT 'BORRADOR',
  `prioridad` ENUM('BAJA', 'MEDIA', 'ALTA', 'URGENTE') NOT NULL DEFAULT 'MEDIA',
  `comentarios` VARCHAR(255) NULL DEFAULT NULL,
  `fecha_creacion` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `fecha_edicion` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id_solicitud`),
  UNIQUE INDEX `folio` (`folio` ASC),
  INDEX `ix_solicitud_estado` (`estado` ASC),
  INDEX `ix_solicitud_usuario` (`id_usuario` ASC),
  CONSTRAINT `fk_solicitud_usuario`
    FOREIGN KEY (`id_usuario`) REFERENCES `usuario` (`id_usuario`)
    ON UPDATE CASCADE ON DELETE RESTRICT
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_general_ci;

-- -----------------------------------------------------
-- Table detalle
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `detalle` (
  `id_detalle` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `id_solicitud` INT UNSIGNED NOT NULL,
  `id_producto` INT UNSIGNED NOT NULL,
  `cantidad` INT NOT NULL,
  `observaciones` VARCHAR(255) NULL DEFAULT NULL,
  PRIMARY KEY (`id_detalle`),
  UNIQUE INDEX `uq_detalle` (`id_solicitud` ASC, `id_producto` ASC),
  INDEX `ix_detalle_producto` (`id_producto` ASC),
  CONSTRAINT `fk_detalle_producto`
    FOREIGN KEY (`id_producto`) REFERENCES `producto` (`id_producto`)
    ON UPDATE CASCADE ON DELETE RESTRICT,
  CONSTRAINT `fk_detalle_solicitud`
    FOREIGN KEY (`id_solicitud`) REFERENCES `solicitud` (`id_solicitud`)
    ON UPDATE CASCADE ON DELETE RESTRICT
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_general_ci;

-- -----------------------------------------------------
-- Table imagen
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `imagen` (
  `id_imagen` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `id_producto` INT UNSIGNED NOT NULL,
  `ruta` VARCHAR(255) NOT NULL,
  `principal` TINYINT NOT NULL DEFAULT '0',
  `descripcion` VARCHAR(255) NULL DEFAULT NULL,
  `fecha_subida` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id_imagen`),
  INDEX `ix_imagen_producto` (`id_producto` ASC),
  CONSTRAINT `fk_imagen_producto`
    FOREIGN KEY (`id_producto`) REFERENCES `producto` (`id_producto`)
    ON UPDATE CASCADE ON DELETE RESTRICT
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_general_ci;

-- -----------------------------------------------------
-- Table proveedor
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `proveedor` (
  `id_proveedor` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `nombre` VARCHAR(160) NOT NULL,
  `rfc` VARCHAR(20) NULL DEFAULT NULL,
  `telefono` VARCHAR(40) NULL DEFAULT NULL,
  `email` VARCHAR(160) NULL DEFAULT NULL,
  `direccion` VARCHAR(255) NULL DEFAULT NULL,
  `activo` TINYINT(1) NOT NULL DEFAULT '1',
  PRIMARY KEY (`id_proveedor`),
  UNIQUE INDEX `nombre` (`nombre` ASC)
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_general_ci;

-- -----------------------------------------------------
-- Table producto_proveedor
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `producto_proveedor` (
  `id_producto` INT UNSIGNED NOT NULL,
  `id_proveedor` INT UNSIGNED NOT NULL,
  `codigo_proveedor` VARCHAR(120) NULL DEFAULT NULL,
  `precio_compra` DECIMAL(12,2) NULL DEFAULT NULL,
  PRIMARY KEY (`id_producto`, `id_proveedor`),
  INDEX `fk_pp_proveedor` (`id_proveedor` ASC),
  CONSTRAINT `fk_pp_producto`
    FOREIGN KEY (`id_producto`) REFERENCES `producto` (`id_producto`)
    ON UPDATE CASCADE ON DELETE RESTRICT,
  CONSTRAINT `fk_pp_proveedor`
    FOREIGN KEY (`id_proveedor`) REFERENCES `proveedor` (`id_proveedor`)
    ON UPDATE CASCADE ON DELETE RESTRICT
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_general_ci;

-- Restaurar checks
-- SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;
